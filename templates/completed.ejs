<div class="row">
	<div class="col s12">
		<ul class="tabs tabs-fixed-width">
			<li class="tab"><a href="#" onclick="apretaste.send({command: 'ENCUESTA'})">Activas</a></li>
			<li class="tab"><a href="#" class="active">Terminadas</a></li> 
		</ul>
	</div>
</div>

<div class="row">
	<div class="col s12">
		<% surveys.forEach(function(item){ %>
			<div class="card">
				<div class="card-content">
					<span class="card-title"><%= item.title %></span>
					<p>Completada el <%= formatDate(item.inserted) %></p>
					<p class="blue-grey-text">Ganastes §<%= item.value %></p>
				</div>
			</div>
		<% }) %>
	</div>
</div>